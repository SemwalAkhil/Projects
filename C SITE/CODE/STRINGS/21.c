// 21. PRINT ALL THE PERMUTATION OF A STRING I.E. "ABCDE".
#include <stdio.h>
#define maxSize 100
void printPerms(char str[maxSize], int size)
{
    char perm[maxSize];
    int count = 0;
    for (int i = 0; i < size; i++)
    {
        
    }
    
}
int main()
{
    char str[maxSize];
    printf("Enter string : ");
    gets(str);
    return 0;
}